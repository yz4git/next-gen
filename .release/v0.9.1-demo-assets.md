# v0.9.1 demo release assets

This marker intentionally triggers the one-time GitHub Actions workflow that publishes a privacy-safe synthetic WorldSeed starter-kit export and a prebuilt static export consumer to the existing v0.9.1 GitHub Release.

The generated demo export contains no exact WGS84 origin.
